# 操作文档

## 一、系统访问

### 1.1 访问地址
- 前端界面：http://114.55.242.250:8080/
- API接口：http://114.55.242.250:8080/api/

### 1.2 登录说明
系统无需登录，直接访问即可使用。

---

## 二、功能操作指南

### 2.1 首页功能

#### 查看系统指标
首页顶部显示4个关键指标卡片：
- **CPU使用率**：实时显示CPU占用百分比
- **内存使用率**：实时显示内存占用百分比
- **请求/秒**：当前每秒请求数
- **活跃连接**：当前活跃连接数

> 指标每5秒自动刷新

#### 查看服务状态
首页中部显示服务卡片列表：
- 绿色边框：服务运行正常
- 红色边框：服务已停止
- 黄色边框：服务状态未知

每个卡片显示：
- 服务名称
- 端口号
- 运行状态

### 2.2 日志功能

#### 查看日志
1. 点击顶部导航栏"📋 日志"按钮
2. 系统自动加载最近50条日志
3. 日志表格显示以下信息：
   - 时间
   - 来源
   - 类型
   - IP地址
   - URL
   - 状态码
   - 消息

#### 日志颜色标识
- 红色背景：5xx错误（服务器错误）
- 黄色背景：4xx错误（客户端错误）
- 白色背景：正常请求

#### 刷新日志
点击"刷新"按钮重新加载日志数据

### 2.3 告警功能

#### 查看告警
1. 点击顶部导航栏"🚨 告警"按钮
2. 系统自动加载最近20条告警
3. 每条告警显示：
   - 告警类型（错误/警告）
   - 状态码
   - 时间戳
   - 消息内容
   - 触发URL
   - AI智能分析

#### 告警颜色标识
- 红色边框：错误类型告警（5xx）
- 黄色边框：警告类型告警（4xx）

#### AI分析内容
每条告警包含AI自动生成的分析：
- 错误原因
- 影响范围
- 修复步骤（1-5步）

### 2.4 测试功能

#### 触发错误
点击顶部按钮测试错误响应：
- **触发404**：模拟页面不存在错误
- **触发500**：模拟服务器内部错误

> 触发后会弹出提示，错误会被记录到日志

#### 测试告警
点击顶部按钮创建测试告警：
- **测试404告警**：创建404类型告警（含AI分析）
- **测试500告警**：创建500类型告警（含AI分析）

> 创建成功后会弹出AI分析内容，并自动刷新告警列表

---

## 三、API接口使用

### 3.1 获取服务列表
```bash
curl http://114.55.242.250:8080/api/services
```

返回示例：
```json
{
  "services": [
    {"name": "web-app", "port": 5000, "status": "running"},
    {"name": "nginx", "port": 80, "status": "running"}
  ]
}
```

### 3.2 获取系统指标
```bash
curl http://114.55.242.250:8080/api/metrics
```

返回示例：
```json
{
  "cpu_usage": 33.5,
  "memory_usage": 26.4,
  "disk_usage": 35.2,
  "requests_per_second": 53,
  "active_connections": 26
}
```

### 3.3 获取日志列表
```bash
curl http://114.55.242.250:8080/api/logs
```

### 3.4 获取告警列表
```bash
curl http://114.55.242.250:8080/api/alerts
```

### 3.5 创建测试告警
```bash
curl -X POST http://114.55.242.250:8080/api/test-alert \
  -H "Content-Type: application/json" \
  -d '{"type": "500", "url": "/test/endpoint"}'
```

返回示例：
```json
{
  "message": "测试告警已创建",
  "alert": {
    "type": "error",
    "status_code": 500,
    "url": "/api/test",
    "ai_analysis": "错误原因：服务器内部错误\n影响范围：用户请求无法正常处理\n修复步骤：\n1. 查看服务器错误日志定位具体问题\n2. 检查数据库连接是否正常\n3. 检查代码逻辑是否存在异常\n4. 重启相关服务\n5. 如问题持续，联系开发人员处理"
  }
}
```

---

## 四、演示流程（5分钟完整演示）

### 步骤1：访问首页（30秒）
1. 打开浏览器访问 http://114.55.242.250:8080/
2. 观察系统指标卡片实时更新
3. 查看服务状态列表

### 步骤2：触发错误（1分钟）
1. 点击"触发404"按钮
2. 点击"触发500"按钮
3. 观察弹出提示

### 步骤3：查看日志（1分钟）
1. 点击"📋 日志"按钮
2. 查看日志列表
3. 观察错误日志的高亮显示

### 步骤4：创建测试告警（1分钟）
1. 点击"测试500告警"按钮
2. 观察弹出的AI分析内容
3. 点击确定

### 步骤5：查看告警（1.5分钟）
1. 点击"🚨 告警"按钮
2. 查看告警列表
3. 阅读AI智能分析结果
4. 理解错误原因和修复步骤

---

## 五、数据验证

### 5.1 验证数据真实性
数据存储在MySQL数据库中，可通过以下命令验证：

```bash
# 连接数据库
docker exec -it subencai-mysql mysql -uroot -proot123 log_monitor

# 查看告警数量
SELECT COUNT(*) FROM alerts;

# 查看所有告警
SELECT * FROM alerts;

# 查看日志数量
SELECT COUNT(*) FROM logs;
```

### 5.2 验证容器状态
```bash
# 查看运行中的容器
docker ps

# 查看容器日志
docker logs log-web
docker logs log-nginx
```

---

## 六、常见问题

### Q1：页面无法访问？
检查容器状态：
```bash
docker ps | grep log
```
如果容器未运行，执行：
```bash
cd /root/log-monitor
docker compose -f docker-compose.simple.yaml up -d
```

### Q2：API返回错误？
检查web-app日志：
```bash
docker logs log-web --tail 50
```

### Q3：告警没有AI分析？
AI分析在创建告警时自动生成，确保使用正确的API调用方式。

### Q4：如何清空数据？
```bash
docker exec -it subencai-mysql mysql -uroot -proot123 log_monitor -e "TRUNCATE TABLE alerts; TRUNCATE TABLE logs;"
```

---

## 七、系统维护

### 7.1 重启服务
```bash
cd /root/log-monitor
docker compose -f docker-compose.simple.yaml restart
```

### 7.2 停止服务
```bash
docker compose -f docker-compose.simple.yaml down
```

### 7.3 启动服务
```bash
docker compose -f docker-compose.simple.yaml up -d
```

### 7.4 更新代码
```bash
# 上传新代码后重新构建
docker compose -f docker-compose.simple.yaml up -d --build
```

---

## 八、技术支持

如遇到问题，请按以下顺序排查：
1. 检查容器状态：`docker ps`
2. 查看容器日志：`docker logs <容器名>`
3. 检查网络连接：`curl http://localhost:8080/api/services`
4. 检查数据库连接：`docker exec subencai-mysql mysql -uroot -proot123 -e "SELECT 1"`
